services:

  website-rag-platform:

    build: .

    container_name: website-rag-platform

    ports:
      - "5000:5000"

    env_file:
      - .env

    volumes:
      - ./database:/app/database
      - ./data:/app/data